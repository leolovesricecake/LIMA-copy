代码项目地址：https://github.com/RuoyuChen10/lima

先全部clone下来，正常配置环境pytorch等，安装CLIP（运行代码缺啥补啥吧）。



数据集：下载ImageNet的测试集，放在：datasets/imagenet/ILSVRC2012_img_val



**注：**如果项目不涉及解释错误样本，dataset list为:`datasets/imagenet/val_clip_vitl_5k_true.txt`

涉及错误样本的list为：`datasets/imagenet/val_clip_vitl_2k_false.txt`

请检查python文件里的路径，可能直接定义，不是args传入。

## 1. 运行HSIC

打开：`baseline_attribution/generate_explanation_maps_clip_vitl.py`

注释157行和169行，解开166行：

``````
explainers = [
        # Saliency(model),
        # GradientInput(model),
        # GuidedBackprop(model),
        # IntegratedGradients(wrapped_model, steps=80, batch_size=batch_size),
        # SmoothGrad(model, nb_samples=80, batch_size=batch_size),
        # SquareGrad(model, nb_samples=80, batch_size=batch_size),
        # VarGrad(model, nb_samples=80, batch_size=batch_size),
        # GradCAM(model),
        # GradCAMPP(model),
        # Occlusion(model, patch_size=10, patch_stride=5, batch_size=batch_size),
        # Rise(model, nb_samples=500, batch_size=batch_size),
        # SobolAttributionMethod(model, batch_size=batch_size),
        HsicAttributionMethod(wrapped_model, batch_size=batch_size),
        # Rise(wrapped_model, nb_samples=500, batch_size=batch_size),
        # Lime(model, nb_samples = 1000),
        # KernelShap(wrapped_model, nb_samples = 1000, batch_size=32)
    ]
``````

然后：

```
python -m baseline_attribution.generate_explanation_maps_clip_vitl --device 1
```

结果请见：`explanation_results`文件夹

然后：

```
python -m baseline_attribution.debug_org_attribution_method_clip_vitl --device 1
```

请自行修改里面的`explanation_method`，然后得到结果在`explanation_insertion_results`

## 2. 运行GradECLIP

打开：`baseline_attribution/generate_explanation_maps_Grad-ECLIP.py`

解开注释30-31行，注释33-34行。如果项目涉及筛选预测错误样本子集，请不要删除

然后：

```
python -m baseline_attribution.generate_explanation_maps_Grad-ECLIP --device 1
```

结果请见：`explanation_results`文件夹

然后：

```
python -m baseline_attribution.debug_org_attribution_method_clip_vitl --device 1
```

请自行修改里面的`explanation_method`，然后得到结果在`explanation_insertion_results`

## 2.5 对HSIC和GradECLIP进行评估：

计算insertion：

```
python -m evals.eval_AUC_faithfulness --explanation-dir explanation_insertion_results/imagenet-clip-vitl-true/GradECLIP

python -m evals.eval_AUC_faithfulness --explanation-dir explanation_insertion_results/imagenet-clip-vitl-true/HsicAttributionMethod
```

计算最高置信度(请自行修改内部参数)：

```
python -m evals.evaluation_mistake_debug_baseline
```

## 3. 本方法

### 针对分类正确样本

```
scripts/efficientv2-clip_vitl_multigpu.sh
```

计算insertion：

```
python -m evals.eval_AUC_faithfulness --explanation-dir submodular_results/imagenet-clip-vitl-efficientv2/seeds-0.0-0.05-20.0-1.0-pending-samples-8
```

计算最高置信度：

```powershell
# 不管执行哪一个都报错
python -m evals.evaluation_mistake_debug_ours
python -m evals.evaluation_mistake_debug_baseline
```

(lima_final) nsq@sysu-sys:/mnt/huawei/nsq/LIMA-copy$ python -m evals.evaluation_mistake_debug_ours
100%|███████████████████████████████████████████████████████████████████████████████████████████████████████████| 4891/4891 [00:00<00:00, 34938.19it/s]
/mnt/huawei/nsq/LIMA-copy/evals/evaluation_mistake_debug_ours.py:56: RuntimeWarning: Mean of empty slice.
  mean_highest_acc = np.array(highest_acc).mean()
/home/nsq/miniconda3/envs/lima_final/lib/python3.8/site-packages/numpy/core/_methods.py:190: RuntimeWarning: invalid value encountered in double_scalars 
  ret = ret.dtype.type(ret / rcount)
/home/nsq/miniconda3/envs/lima_final/lib/python3.8/site-packages/numpy/core/_methods.py:265: RuntimeWarning: Degrees of freedom <= 0 for slice
  ret = _var(a, axis=axis, dtype=dtype, out=out, ddof=ddof,
/home/nsq/miniconda3/envs/lima_final/lib/python3.8/site-packages/numpy/core/_methods.py:223: RuntimeWarning: invalid value encountered in divide
  arrmean = um.true_divide(arrmean, div, out=arrmean, casting='unsafe',
/home/nsq/miniconda3/envs/lima_final/lib/python3.8/site-packages/numpy/core/_methods.py:257: RuntimeWarning: invalid value encountered in double_scalars 
  ret = ret.dtype.type(ret / rcount)
/mnt/huawei/nsq/LIMA-copy/evals/evaluation_mistake_debug_ours.py:59: RuntimeWarning: Mean of empty slice.
  mean_region_area = np.array(region_area).mean()
When percentage is 0.25, the avg. highest confidence is nan, std:nan, the retention percentage at highest confidence is nan, std:nan
100%|███████████████████████████████████████████████████████████████████████████████████████████████████████████| 4891/4891 [00:00<00:00, 65313.37it/s]
When percentage is 0.5, the avg. highest confidence is nan, std:nan, the retention percentage at highest confidence is nan, std:nan
100%|███████████████████████████████████████████████████████████████████████████████████████████████████████████| 4891/4891 [00:00<00:00, 72013.24it/s]
When percentage is 0.75, the avg. highest confidence is nan, std:nan, the retention percentage at highest confidence is nan, std:nan
100%|███████████████████████████████████████████████████████████████████████████████████████████████████████████| 4891/4891 [00:00<00:00, 50581.63it/s]
When percentage is 1.0, the avg. highest confidence is nan, std:nan, the retention percentage at highest confidence is nan, std:nan


### 针对分类错误样本

```
scripts/efficientv2-clip_vitl_multigpu_debug.sh
```

计算insertion：

```
python -m evals.eval_AUC_faithfulness --explanation-dir submodular_results/imagenet-clip-vitl-efficientv2/slico-0.0-0.05-10.0-1.0-pending-samples-8
```

计算最高置信度，先手动修改：
- `eval_list` 为 `datasets/imagenet/val_clip_vitl_2k_false.txt`
- `explanation_method` 为 ……
- 然后执行：
```
python -m evals.evaluation_mistake_debug_baseline
```
